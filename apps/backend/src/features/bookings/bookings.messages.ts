import { MESSAGE_KEYS } from '@shared/constants/message-keys.js';

export const BOOKING_MESSAGES = {
  CREATED: MESSAGE_KEYS.BOOKING_CREATED_OK,
  LIST_FETCHED: MESSAGE_KEYS.BOOKINGS_LIST_FETCHED,
  FETCHED: MESSAGE_KEYS.BOOKING_FETCHED,
  CANCELLED: MESSAGE_KEYS.BOOKING_CANCELLED_OK,
  NOT_FOUND: MESSAGE_KEYS.BOOKING_NOT_FOUND_KEY,
  CONFLICT: MESSAGE_KEYS.BOOKING_CONFLICT,
  INVALID: MESSAGE_KEYS.BOOKING_INVALID,
} as const;
