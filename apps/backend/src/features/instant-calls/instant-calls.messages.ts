import { MESSAGE_KEYS } from '@shared/constants/message-keys.js';

export const INSTANT_CALL_MESSAGES = {
  STARTED: MESSAGE_KEYS.INSTANT_CALL_STARTED,
  ANSWERED: MESSAGE_KEYS.INSTANT_CALL_ANSWERED,
  ENDED: MESSAGE_KEYS.INSTANT_CALL_ENDED,
  PAUSED: MESSAGE_KEYS.INSTANT_CALL_PAUSED,
  RESUMED: MESSAGE_KEYS.INSTANT_CALL_RESUMED,
  NOT_ACTIVE: MESSAGE_KEYS.INSTANT_CALL_NOT_ACTIVE,
  NOT_FOUND: MESSAGE_KEYS.INSTANT_CALL_NOT_FOUND,
  NOT_RINGING: MESSAGE_KEYS.INSTANT_CALL_NOT_RINGING,
  NO_MINUTES: MESSAGE_KEYS.INSTANT_CALL_NO_MINUTES,
  OFFLINE: MESSAGE_KEYS.INSTANT_CALL_OFFLINE,
  NOT_ACCEPTING: MESSAGE_KEYS.INSTANT_CALL_NOT_ACCEPTING,
  DND: MESSAGE_KEYS.INSTANT_CALL_DND,
  BUSY: MESSAGE_KEYS.INSTANT_CALL_BUSY,
  CANNOT_CALL_SELF: MESSAGE_KEYS.INSTANT_CALL_CANNOT_CALL_SELF,
  HISTORY_FETCHED: MESSAGE_KEYS.INSTANT_CALL_HISTORY_FETCHED,
} as const;

export const CALL_INVITE_MESSAGES = {
  CREATED: MESSAGE_KEYS.CALL_INVITE_CREATED,
  APPROVED: MESSAGE_KEYS.CALL_INVITE_APPROVED,
  DECLINED: MESSAGE_KEYS.CALL_INVITE_DECLINED,
  ACCEPTED: MESSAGE_KEYS.CALL_INVITE_ACCEPTED,
  NOT_FOUND: MESSAGE_KEYS.CALL_INVITE_NOT_FOUND,
  FORBIDDEN: MESSAGE_KEYS.CALL_INVITE_FORBIDDEN,
  ROOM_FULL: MESSAGE_KEYS.CALL_INVITE_ROOM_FULL,
  INVALID_TARGET: MESSAGE_KEYS.CALL_INVITE_INVALID_TARGET,
  ALREADY_RESOLVED: MESSAGE_KEYS.CALL_INVITE_ALREADY_RESOLVED,
  PARTICIPANTS_LISTED: MESSAGE_KEYS.CALL_PARTICIPANTS_LISTED,
} as const;
