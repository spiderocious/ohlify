import { MESSAGE_KEYS } from '@shared/constants/message-keys.js';

export const NOTIFICATION_MESSAGES = {
  LISTED: MESSAGE_KEYS.NOTIFICATIONS_LISTED,
  MARKED_READ: MESSAGE_KEYS.NOTIFICATIONS_MARKED_READ,
  NOT_FOUND: MESSAGE_KEYS.NOTIFICATIONS_NOT_FOUND,
  BADGES_FETCHED: MESSAGE_KEYS.BADGES_FETCHED,
  SURFACE_SEEN: MESSAGE_KEYS.SURFACE_SEEN,
} as const;
