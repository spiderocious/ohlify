import { MESSAGE_KEYS } from '@shared/constants/message-keys.js';

export const REFUND_MESSAGES = {
  CREATED: MESSAGE_KEYS.REFUND_CREATED,
  LIST_FETCHED: MESSAGE_KEYS.REFUNDS_LIST_FETCHED,
  FETCHED: MESSAGE_KEYS.REFUND_FETCHED,
  APPROVED: MESSAGE_KEYS.REFUND_APPROVED,
  REJECTED: MESSAGE_KEYS.REFUND_REJECTED,
  NOT_FOUND: MESSAGE_KEYS.REFUND_NOT_FOUND,
  CONFLICT: MESSAGE_KEYS.REFUND_CONFLICT,
  INVALID_TARGET: MESSAGE_KEYS.REFUND_INVALID_TARGET,
} as const;
